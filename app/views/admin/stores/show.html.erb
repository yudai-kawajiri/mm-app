<%# 店舗詳細画面 %>
<%= render 'shared/details/resource_show',
  resource: @store,
  title: t('admin.stores.show.title'),
  content_left_partial: 'admin/stores/details',
  content_left_title: nil,
  partial_locals: { store: @store },
  scope: :admin,
  show_edit_button: !current_user.store_admin?,
  show_delete_button: !current_user.store_admin? %>
