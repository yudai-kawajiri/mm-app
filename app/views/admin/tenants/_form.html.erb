<%# テナントフォームフィールド %>
<div class="row">
  <div class="col-md-6">
    <%= form_group_lg(f, :name,
        required: true,
        label: raw([t('activerecord.attributes.tenant.name'), t('helpers.required_mark')].join(' ')),
        placeholder: t('admin.tenants.form.name_placeholder')
    ) %>
  </div>
  <div class="col-md-6">
    <%= form_group_lg(f, :subdomain,
        required: true,
        label: raw([t('activerecord.attributes.tenant.subdomain'), t('helpers.required_mark')].join(' ')),
        placeholder: t('admin.tenants.form.subdomain_placeholder'),
        help_text: t('admin.tenants.form.subdomain_hint')
    ) %>
  </div>
</div>
