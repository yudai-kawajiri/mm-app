<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #007bff; color: white; padding: 20px; border-radius: 5px; margin-bottom: 20px; text-align: center; }
    .content { background-color: #fff; padding: 20px; border: 1px solid #dee2e6; border-radius: 5px; }
    .info-box { background-color: #f8f9fa; padding: 15px; border-left: 4px solid #007bff; margin: 20px 0; }
    .field { margin-bottom: 10px; }
    .label { font-weight: bold; color: #495057; }
    .button { display: inline-block; padding: 12px 30px; background-color: #28a745; color: white; text-decoration: none; border-radius: 5px; margin: 20px 0; }
    .footer { margin-top: 30px; padding-top: 20px; border-top: 1px solid #dee2e6; text-align: center; font-size: 12px; color: #6c757d; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h2><%= t('application_request_mailer.invitation_email.header') %></h2>
    </div>

    <div class="content">
      <p><%= t('application_request_mailer.invitation_email.greeting', name: @application_request.contact_name) %></p>
      
      <p><%= t('application_request_mailer.invitation_email.introduction') %></p>
      
      <div class="info-box">
        <h3 style="margin-top: 0;"><%= t('application_request_mailer.invitation_email.company_info') %></h3>
        <div class="field">
          <span class="label"><%= t('application_request_mailer.invitation_email.company_name') %>:</span> <%= @application_request.company_name %>
        </div>
        <div class="field">
          <span class="label"><%= t('application_request_mailer.invitation_email.email') %>:</span> <%= @application_request.company_email %>
        </div>
        <% if @application_request.company_phone.present? %>
          <div class="field">
            <span class="label"><%= t('application_request_mailer.invitation_email.phone') %>:</span> <%= @application_request.company_phone %>
          </div>
        <% end %>
      </div>
      
      <p><%= t('application_request_mailer.invitation_email.instruction') %></p>
      
      <div style="text-align: center;">
        <a href="<%= @invitation_url %>" class="button"><%= t('application_request_mailer.invitation_email.action') %></a>
      </div>
      
      <p style="font-size: 14px; color: #6c757d;">
        <%= t('application_request_mailer.invitation_email.expiry_notice') %>
      </p>
      
      <p style="font-size: 14px; color: #6c757d;">
        <%= t('application_request_mailer.invitation_email.fallback_instruction') %><br>
        <%= @invitation_url %>
      </p>
      
      <div class="footer">
        <p><%= t('application_request_mailer.invitation_email.footer_notice') %></p>
        <p><%= t('application_request_mailer.invitation_email.copyright', year: Time.current.year) %></p>
      </div>
    </div>
  </div>
</body>
</html>
