<%= render 'shared/forms/resource_form',
    resource: @category,
    title: t('categories.edit.title', name: @category.name),
    basic_fields: 'categories/category_fields',
    resource_name: 'categories'
%>
