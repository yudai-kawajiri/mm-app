<% content_for :title, t('categories.new.title') %>

<%= render 'shared/resource_form',
           resource: @category,
           form_title: t('categories.new.title'),
           submit_label: t('helpers.submit.create') %>
