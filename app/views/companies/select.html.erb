<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <h1 class="mb-4">会社選択</h1>
      
      <% if @companies.empty? %>
        <div class="alert alert-warning">
          所属する会社が見つかりませんでした。管理者にお問い合わせください。
        </div>
      <% else %>
        <p class="text-muted mb-3">アクセスする会社を選択してください:</p>
        <div class="list-group">
          <% @companies.each do |company| %>
            <%= link_to company_dashboards_path(company_slug: company.slug), class: "list-group-item list-group-item-action" do %>
              <h5 class="mb-1"><%= company.name %></h5>
              <small class="text-muted"><%= company.slug %></small>
            <% end %>
          <% end %>
        </div>
      <% end %>
      
      <div class="mt-3">
        <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-outline-secondary" %>
      </div>
    </div>
  </div>
</div>
