<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #f8f9fa; padding: 20px; border-radius: 5px; margin-bottom: 20px; }
    .content { background-color: #fff; padding: 20px; border: 1px solid #dee2e6; border-radius: 5px; }
    .field { margin-bottom: 15px; }
    .label { font-weight: bold; color: #495057; }
    .value { margin-top: 5px; padding: 10px; background-color: #f8f9fa; border-radius: 3px; }
    .message { white-space: pre-wrap; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h2><%= t('contact_mailer.contact_email.title') %></h2>
    </div>

    <div class="content">
      <div class="field">
        <div class="label"><%= t('contact_mailer.contact_email.name_label') %>:</div>
        <div class="value"><%= @contact.name %></div>
      </div>

      <div class="field">
        <div class="label"><%= t('contact_mailer.contact_email.email_label') %>:</div>
        <div class="value"><%= @contact.email %></div>
      </div>

      <div class="field">
        <div class="label"><%= t('contact_mailer.contact_email.subject_label') %>:</div>
        <div class="value"><%= @contact.subject %></div>
      </div>

      <div class="field">
        <div class="label"><%= t('contact_mailer.contact_email.message_label') %>:</div>
        <div class="value message"><%= @contact.message %></div>
      </div>
    </div>
  </div>
</body>
</html>
