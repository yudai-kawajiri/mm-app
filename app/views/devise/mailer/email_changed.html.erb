<%# メールアドレス変更通知 %>
<p><%= t('devise.mailer.email_changed.greeting', email: @email) %></p>

<% if @resource.try(:unconfirmed_email?) %>
  <p><%= t('devise.mailer.email_changed.message_unconfirmed', unconfirmed_email: @resource.unconfirmed_email) %></p>
<% else %>
  <p><%= t('devise.mailer.email_changed.message_confirmed', email: @resource.email) %></p>
<% end %>
