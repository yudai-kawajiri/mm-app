<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #007bff; color: white; padding: 20px; border-radius: 5px; margin-bottom: 20px; text-align: center; }
    .content { background-color: #fff; padding: 20px; border: 1px solid #dee2e6; border-radius: 5px; }
    .button { display: inline-block; padding: 12px 30px; background-color: #007bff; color: white; text-decoration: none; border-radius: 5px; margin: 20px 0; }
    .footer { margin-top: 30px; padding-top: 20px; border-top: 1px solid #dee2e6; text-align: center; font-size: 12px; color: #6c757d; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h2><%= t('devise.mailer.reset_password_instructions.subject') %></h2>
    </div>

    <div class="content">
      <p><%= t('devise.mailer.reset_password_instructions.greeting') %></p>
      
      <p><%= t('devise.mailer.reset_password_instructions.instruction') %></p>
      
      <div style="text-align: center;">
        <%= link_to t('devise.mailer.reset_password_instructions.action'), 
            company_edit_user_password_url(company_slug: @resource.company.slug, reset_password_token: @token), 
            class: 'button' %>
      </div>
      
      <p style="font-size: 14px; color: #6c757d;">
        <%= t('devise.mailer.reset_password_instructions.instruction_2') %><br>
        <%= company_edit_user_password_url(company_slug: @resource.company.slug, reset_password_token: @token) %>
      </p>
      
      <p style="font-size: 14px; color: #6c757d;">
        <%= t('devise.mailer.reset_password_instructions.instruction_3') %>
      </p>
      
      <div class="footer">
        <p>&copy; <%= Time.current.year %> MM-App. All rights reserved.</p>
      </div>
    </div>
  </div>
</body>
</html>
