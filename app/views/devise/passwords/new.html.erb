<%# パスワードリセット %>
<%#
  機能:
  - パスワードリセット用のメール送信フォーム
  - 登録メールアドレスにリセットリンクを送信
%>
<% content_for :title, t('devise.passwords.new.title') %>
<%= render "devise/shared/auth_card_frame" do %>
  <div class="mb-4 text-center">
    <h3 class="fw-bold text-primary mb-2">
      <%= t('devise.passwords.new.title') %>
    </h3>
    <p class="text-muted mb-0"><%= t('devise.passwords.new.description') %></p>
  </div>

  <%= render "devise/shared/error_messages", resource: resource %>

  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: 'needs-validation', novalidate: true }) do |f| %>
    <div class="mb-4">
      <%= f.label :email, class: 'form-label fw-bold' do %>
        <%= t('activerecord.attributes.user.email') %>
      <% end %>
      <%= f.email_field :email,
          autofocus: true,
          autocomplete: "email",
          class: 'form-control form-control-lg',
          placeholder: t('devise.passwords.new.email_placeholder'),
          required: true %>
      <div class="form-text">
        <%= t('devise.passwords.new.email_help') %>
      </div>
    </div>

    <div class="d-grid gap-2 mb-3">
      <%= f.submit t('devise.passwords.new.submit'), class: 'btn btn-primary btn-lg' %>
    </div>

    <div class="text-center">
      <%= link_to new_session_path(resource_name), class: 'text-decoration-none' do %>
        <i class="bi bi-arrow-left me-1"></i><%= t('devise.shared.links.sign_in') %>
      <% end %>
    </div>
  <% end %>
<% end %>
