<%# パーシャルでインスタンス変数をmaterialで扱える %>
<%= render 'shared/form_wrapper',
    title: t('.title', material_name: @material.name),
    resource: @material,
    back_path: @material do |f| %>

  <%# Material固有のフォーム項目を挿入 %>
  <%= render 'material_fields', f: f %>
<% end %>