<%# content_for や <h2> タグはラッパーに委譲するため削除 %>

<%# 共通パーシャル参照し@materialをresourceとして渡す %>
<%= render 'shared/form_wrapper',
    title: t('.title'),
    resource: @material,
    back_path: materials_path do |f| %>

  <%# Material固有のフォーム項目を挿入 %>
  <%= render 'material_fields', f: f %>
<% end %>