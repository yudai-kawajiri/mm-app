<%= render 'shared/forms/resource_form',
    resource: @plan,
    title: t('plans.edit.title', name: @plan.name),
    basic_fields: 'plans/plan_fields',
    detail_fields: 'plans/plan_products_fields',
    resource_name: 'plans',
    form_html: { id: 'plan_form' },
    form_data: { controller: 'form-submit' }
%>
