<% content_for(:title, t('plans.edit.title', name: @plan.name)) %>

<%= render 'shared/resource_two_column_form_wrapper',
  resource: @plan,
  form_title: t('plans.edit.title', name: @plan.name),
  basic_fields: 'plans/plan_fields',
  detail_fields: 'plans/plan_products_fields',
  custom_right_title: t('forms.titles.plan_product')
%>
