<%#
  機能: 商品新規作成画面
  - 新しい商品を作成
%>
<% content_for(:title, t('products.new.title')) %>

<div class="container mt-4">
  <div class="card shadow-sm">
    <div class="card-header bg-primary text-white">
      <h2 class="mb-0"><%= t('products.new.title') %></h2>
    </div>

    <div class="card-body">
      <%= render 'form', product: @product %>
    </div>
  </div>
</div>
