<%# カテゴリー編集画面 %>
<% content_for :title, t('categories.edit.title', name: @category.name) %>

<%= render 'shared/resource_form',
            resource: @category,
            form_title: t('categories.edit.title', name: @category.name),
            submit_label: t('common.update') %>
