<%# 発注グループ編集画面 %>
<% content_for :title, t('material_order_groups.edit.title', name: @material_order_group.name) %>

<%= render 'shared/resource_form',
            resource: @material_order_group,
            form_title: t('material_order_groups.edit.title', name: @material_order_group.name),
            submit_label: t('common.update') %>
