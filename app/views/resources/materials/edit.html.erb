<%# 原材料編集画面 %>
<% content_for :title, t('materials.edit.title', name: @material.name) %>

<%= render 'shared/resource_form',
            resource: @material,
            form_title: t('materials.edit.title', name: @material.name),
            submit_label: t('common.update') %>
