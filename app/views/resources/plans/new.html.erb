<%#
  機能: 製造計画新規作成画面
  - 新しい製造計画を作成
%>
<% content_for(:title, t('plans.new.title')) %>

<div class="container mt-4">
  <div class="card shadow-sm">
    <div class="card-header bg-primary text-white">
      <h2 class="mb-0"><%= t('plans.new.title') %></h2>
    </div>

    <div class="card-body">
      <%= render 'form', plan: @plan %>
    </div>
  </div>
</div>
