<%# キャンセルリンク %>
<% url = local_assigns[:url] %>

<% label = local_assigns.fetch(:label, t('helpers.link.cancel')) %>
<% css_class = local_assigns.fetch(:class, 'btn btn-secondary') %>

<%= link_to label, url, class: css_class %>
