<% if can_create_store_resource? %>
  <%= link_to t('helpers.link.new'), url, class: 'btn btn-primary' %>
<% else %>
  <button class='btn btn-primary' disabled>
    <%= t('helpers.link.new') %>
  </button>
<% end %>
