<%= content_for(:title, t('units.new.title')) %>
<% title = t('.title') %>

<%= render 'shared/layouts/header_with_actions',
    title: title,
    resource: @unit,
    is_form_page: true,
    is_index_page: false,
    is_show_page: false
%>

<%= form_with model: @unit, local: true do |f| %>

  <%= render 'shared/resource_two_column_form',
    f: f,
    basic_fields: 'units/unit_fields',
    detail_fields: nil,
    resource_name: 'units'
  %>

  <div style="display:none;">
    <% submit_text = f.object.persisted? ? t('helpers.submit.update') : t('helpers.submit.create') %>
    <%= f.submit submit_text, id: "hidden-submit-#{dom_id(f.object)}" %>
  </div>
<% end %>