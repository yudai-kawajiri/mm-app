set -e

echo "=========================================="
echo "マイグレーションを実行中..."
echo "=========================================="
bundle exec rails db:migrate

echo "=========================================="
echo "シード処理を開始します..."
echo "=========================================="
bundle exec rails db:seed

echo "=========================================="
echo "デプロイ後処理完了"
echo "=========================================="

# Puma を起動
exec bundle exec puma -C config/puma.rb
